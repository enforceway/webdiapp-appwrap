import React from 'react';
import { Text, View, Image } from 'react-native';
import LotsOfGreetings from './src/index'

export default class HelloWorldApp extends React.Component {
  render() {
    return (
      <LotsOfGreetings />
    );
  }
}